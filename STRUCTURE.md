# Структура проекта после миграции UI

## 📁 Организация файлов

### CSS файлы (`css/`)
```
css/
├── styles.css              # Основные стили приложения (1,695 строк)
├── inline-styles.css       # Встроенные стили (scrollbar-theme, buttons-radius-fix)
├── main.css                # Главный файл для импортов (опционально)
├── base/
│   ├── variables.css       # CSS переменные
│   └── base.css           # Базовые стили
└── README.md              # Документация
```

**Подключение в index.html:**
```html
<link rel="stylesheet" href="css/inline-styles.css" />
<link rel="stylesheet" href="css/styles.css" />
```

### HTML шаблоны (`templates/`)
```
templates/
├── components/
│   ├── header.html        # Шапка приложения
│   └── tabs.html          # Навигационные табы
├── modals/
│   ├── algorithm-modal.html
│   ├── edit-modal.html
│   ├── add-modal.html
│   ├── hotkeys-modal.html
│   ├── cib-link-modal.html
│   ├── confirm-clear-data-modal.html
│   └── customize-ui-modal.html
└── README.md              # Документация
```

### JavaScript UI компоненты (`js/ui/`)
```
js/ui/
└── template-loader.js     # Утилита для динамической загрузки шаблонов
```

## 📊 Статистика миграции

- **index.html**: уменьшен с 4,415 до 2,247 строк (**-49%**)
- **CSS**: вынесено в отдельные файлы (1,695 + 98 строк)
- **HTML шаблоны**: 9 файлов извлечено

## 🔗 Связи между файлами

1. `index.html` → подключает `css/inline-styles.css` и `css/styles.css`
2. `index.html` → содержит модальные окна (для работы при загрузке)
3. `templates/` → готовы для использования через `template-loader.js`
4. `css/main.css` → можно использовать для импорта всех стилей через `@import`

## 💡 Рекомендации по использованию

### Использование шаблонов
```javascript
import { loadTemplate, loadTemplateIntoElement } from './js/ui/template-loader.js';

// Загрузить шаблон
const html = await loadTemplate('modals/algorithm-modal.html');

// Загрузить и вставить в элемент
await loadTemplateIntoElement('modals/algorithm-modal.html', '#container');
```

### Использование CSS модулей
Вместо подключения отдельных файлов можно использовать `css/main.css`:
```html
<link rel="stylesheet" href="css/main.css" />
```

Но учтите, что `@import` может быть медленнее, чем прямые ссылки.
