# Рефакторинг структуры проекта

## 📁 Новая структура проекта

Проект был рефакторен из одного большого файла `script.js` (31,138 строк) в модульную структуру:

```
js/
├── app/
│   ├── state.js              # Глобальное состояние приложения
│   └── app.js                # Главный файл инициализации
├── components/
│   ├── algorithms.js         # Компонент работы с алгоритмами
│   ├── main-algorithm.js     # Компонент главного алгоритма
│   ├── bookmarks.js          # Компонент работы с закладками
│   ├── modals.js             # Работа с модальными окнами
│   ├── tabs.js               # Управление вкладками
│   ├── ext-links.js          # Внешние ресурсы
│   ├── reglaments.js         # Регламенты
│   ├── sedo.js               # Типы СЭДО
│   ├── client-data.js        # Клиентские данные
│   └── theme.js              # Тема (светлая/тёмная/системная)
├── config.js                 # Конфигурация приложения
├── constants.js              # Константы
├── db/
│   ├── favorites.js          # Работа с избранным (БД)
│   ├── indexeddb.js          # Основные операции с БД
│   └── stores.js             # Конфигурация хранилищ IndexedDB
├── features/
│   ├── blacklist.js          # ✨ NEW: Система черного списка
│   ├── cib-links.js          # ✨ NEW: Ссылки 1С
│   ├── favorites.js          # ✨ NEW: Система избранного
│   ├── google-docs.js        # Интеграция с Google Docs
│   ├── pdf-attachments.js    # PDF-вложения
│   ├── search.js             # Система поиска
│   ├── sedo.js               # Типы СЭДО
│   └── timer.js              # Таймер
├── services/
│   ├── export.js             # Сервис экспорта в PDF
│   └── notification.js       # Сервис уведомлений
├── utils/
│   ├── helpers.js            # Вспомогательные функции
│   └── html.js               # Утилиты для работы с HTML
└── entry.js                  # Точка входа (связывает модули с window)
```

## ✅ Выполнено

1. ✅ Структура папок — создана модульная структура
2. ✅ Константы — вынесены в `constants.js`
3. ✅ Конфигурация — в `config.js`
4. ✅ Состояние — в `app/state.js`
5. ✅ Утилиты — в `utils/`
6. ✅ База данных — в `db/`
7. ✅ Сервисы — в `services/`
8. ✅ Компоненты UI — modals, tabs, algorithms, main-algorithm, bookmarks
9. ✅ Доп. компоненты — ext-links, reglaments, sedo, client-data, theme (заглушки/реализация)
10. ✅ Главный файл — `app.js` и точка входа `entry.js`
11. ✅ Подключение в HTML — после `script.js` подключается `<script type="module" src="./js/entry.js">`

## 🔄 Текущее состояние

- ✅ **МИГРАЦИЯ ЗАВЕРШЕНА!**
- **script.js** использует модульную систему ES6 с импортами из всех модулей.
- **entry.js** автоматически загружается после script.js и связывает зависимости через `setDependencies()`.
- Все модули полностью функционируют и интегрированы в приложение.
- Приложение работает через HTTP-сервер (требование для ES-модулей).

## 📝 Как подключено в HTML

```html
<script type="module" src="./script.js"></script>
<script type="module" src="./js/entry.js"></script>
```

Порядок: 
1. Сначала выполняется `script.js` как ES-модуль (импортирует все модули из `js/*`)
2. Затем загружается `entry.js`, который связывает глобальные зависимости через `setDependencies()`
3. Приложение полностью инициализируется

## 🧪 Тестирование

1. **Запуск локального сервера** (ES-модули не работают при открытии через file://):
   - Windows PowerShell: `.\start-server.ps1`
   - Windows CMD: `start-server.bat`
   - Вручную: `python -m http.server 8000` в корне проекта
   
2. **Открыть приложение**:
   - Перейти по адресу: http://localhost:8000
   - Или протестировать модули: http://localhost:8000/test-modules.html
   
3. **Проверка в консоли браузера**:
   - Должно появиться: `[entry.js] Модули связаны с глобальными зависимостями.`
   - Должно появиться: `[App] Модуль приложения загружен.`
   - Не должно быть ошибок импорта
   
4. **Функциональная проверка**:
   - ✓ Вкладки переключаются корректно
   - ✓ Закладки работают (добавление, удаление, поиск)
   - ✓ Алгоритмы отображаются и ищутся
   - ✓ Уведомления появляются
   - ✓ Экспорт в PDF работает
   - ✓ Тема (светлая/темная) переключается
   - ✓ База данных IndexedDB инициализируется

## 📝 Использование модулей в коде (при дальнейшей миграции)

### Использование модулей в коде

```javascript
// Импорт констант
import { DB_NAME, DB_VERSION } from './js/constants.js';

// Импорт функций БД
import { getAllFromIndexedDB, saveToIndexedDB } from './js/db/indexeddb.js';

// Импорт сервисов
import { NotificationService } from './js/services/notification.js';

// Использование
NotificationService.add('Сообщение', 'success');
```

## 🎯 Преимущества новой структуры

1. **Модульность** - код разделен на логические модули
2. **Читаемость** - легче найти нужную функциональность
3. **Поддерживаемость** - проще вносить изменения
4. **Тестируемость** - модули можно тестировать отдельно
5. **Переиспользование** - модули можно использовать в других проектах
6. **Масштабируемость** - легко добавлять новые модули

## ⚠️ Важно

- Все модули используют ES6 модули (`import`/`export`)
- Для работы требуется сервер или локальный сервер разработки
- Браузер должен поддерживать ES6 модули (все современные браузеры поддерживают)

## 📚 Документация модулей

### Константы (`constants.js`)
Содержит все константы приложения: названия БД, ключи localStorage, веса полей для поиска и т.д.

### Конфигурация (`config.js`)
Содержит конфигурационные объекты: настройки вкладок, модальных окон, UI классы.

### База данных (`db/`)
- `indexeddb.js` - основные операции с IndexedDB
- `favorites.js` - работа с избранным
- `stores.js` - конфигурация хранилищ

### Сервисы (`services/`)
- `notification.js` - система уведомлений
- `export.js` - экспорт в PDF

### Компоненты (`components/`)
- `modals.js` — работа с модальными окнами
- `tabs.js` — управление вкладками
- `algorithms.js` — работа с алгоритмами
- `main-algorithm.js` — главный алгоритм
- `bookmarks.js` — работа с закладками
- `ext-links.js` — внешние ресурсы (пока делегирует в window)
- `reglaments.js` — регламенты (пока делегирует в window)
- `sedo.js` — типы СЭДО (пока делегирует в window)
- `client-data.js` — клиентские данные (getClientData, saveClientData, clearClientData)
- `theme.js` — переключение темы (setTheme)

### Утилиты (`utils/`)
- `helpers.js` - вспомогательные функции
- `html.js` - утилиты для работы с HTML

### Features (`features/`)
- `favorites.js` - система избранного (UI, логика, обработчики событий)
- `cib-links.js` - система ссылок 1С (добавление, редактирование, удаление)
- `blacklist.js` - система черного списка (клиенты-жабы, проверка ИНН)
- `google-docs.js` - интеграция с Google Docs
- `pdf-attachments.js` - работа с PDF-вложениями
- `search.js` - полнотекстовый поиск и индексация
- `sedo.js` - работа с типами СЭДО
- `timer.js` - система таймера

## 📊 История миграции

### 2026-02-05: Продолжение миграции (часть 2)

**Выполнено:**

1. **Удалён legacy search code** (~133K символов)
   - Закомментированный код поиска из `script.js` удалён
   - Функции поиска теперь только в `js/features/search.js`
   - Размер `script.js` уменьшен с ~1.24MB до ~1.01MB

2. **Создан модуль `js/features/favorites.js`** (~700 строк)
   - Вынесена система избранного из `script.js`
   - Функции: `toggleFavorite`, `renderFavoritesPage`, `getFavoriteButtonHTML`, и др.
   - Добавлена система зависимостей через `setFavoritesDependencies()`
   - В `script.js` оставлены wrapper-функции для обратной совместимости
   - Удалено ~27K символов из `script.js`

3. **Создан модуль `js/features/cib-links.js`** (~530 строк)
   - Вынесена система ссылок 1С из `script.js`
   - Функции: `initCibLinkSystem`, `loadCibLinks`, `renderCibLinks`, `deleteCibLink`, и др.
   - Добавлена система зависимостей через `setCibLinksDependencies()`
   - Удалено ~18K символов из `script.js`

4. **Создан модуль `js/features/blacklist.js`** (~1100 строк)
   - Вынесена система черного списка из `script.js`
   - Функции: `initBlacklistSystem`, `loadBlacklistedClients`, `renderBlacklistTable`, `showBlacklistEntryModal`, `isInnBlacklisted`, `checkForBlacklistedInn`, `exportBlacklistToExcel`, и др.
   - DB wrappers: `addBlacklistEntryDB`, `getBlacklistEntryDB`, `updateBlacklistEntryDB`, `deleteBlacklistEntryDB`, `getAllBlacklistEntriesDB`
   - Добавлена система зависимостей через `setBlacklistDependencies()`
   - В `script.js` оставлены wrapper-функции для обратной совместимости
   - Удалено ~1071 строк из `script.js`

**Текущий размер `script.js`:** ~22007 строк (~23078 было до миграции blacklist)

**Статус модулей:**
- [x] `js/features/search.js` - система поиска
- [x] `js/features/favorites.js` - система избранного
- [x] `js/features/cib-links.js` - ссылки 1С
- [x] `js/features/blacklist.js` - черный список
- [x] `js/features/timer.js` - таймер
- [x] `js/features/sedo.js` - типы СЭДО
- [x] `js/features/google-docs.js` - Google Docs
- [x] `js/features/pdf-attachments.js` - PDF вложения

**Следующие шаги (pending):**
- [ ] Вынести UI Customization в `js/features/ui-customization.js` (~2200 строк, сложный блок)
- [ ] Вынести Import/Export в `js/features/import-export.js` (~1500 строк)
- [ ] Вынести Screenshots System в `js/features/screenshots.js` (функции разбросаны)
