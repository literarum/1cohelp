# Рефакторинг структуры проекта

## 📁 Новая структура проекта

Проект был рефакторен из одного большого файла `script.js` (31,138 строк) в модульную структуру:

```
js/
├── app/
│   ├── state.js              # Глобальное состояние приложения
│   └── app.js                # Главный файл инициализации
├── components/
│   ├── algorithms.js        # Компонент работы с алгоритмами
│   ├── main-algorithm.js     # Компонент главного алгоритма
│   ├── bookmarks.js          # Компонент работы с закладками
│   ├── modals.js             # Работа с модальными окнами
│   ├── tabs.js               # Управление вкладками
│   ├── ext-links.js          # Внешние ресурсы (заглушка → window)
│   ├── reglaments.js         # Регламенты (заглушка → window)
│   ├── sedo.js               # Типы СЭДО (заглушка → window)
│   ├── client-data.js        # Клиентские данные (информация по обращению)
│   └── theme.js              # Тема (светлая/тёмная/системная)
├── config.js                 # Конфигурация приложения
├── constants.js               # Константы
├── db/
│   ├── favorites.js          # Работа с избранным
│   ├── indexeddb.js          # Основные операции с БД
│   └── stores.js             # Конфигурация хранилищ IndexedDB
├── services/
│   ├── export.js             # Сервис экспорта в PDF
│   └── notification.js       # Сервис уведомлений
├── utils/
│   ├── helpers.js            # Вспомогательные функции
│   └── html.js               # Утилиты для работы с HTML
└── entry.js                  # Точка входа (связывает модули с window)
```

## ✅ Выполнено

1. ✅ Структура папок — создана модульная структура
2. ✅ Константы — вынесены в `constants.js`
3. ✅ Конфигурация — в `config.js`
4. ✅ Состояние — в `app/state.js`
5. ✅ Утилиты — в `utils/`
6. ✅ База данных — в `db/`
7. ✅ Сервисы — в `services/`
8. ✅ Компоненты UI — modals, tabs, algorithms, main-algorithm, bookmarks
9. ✅ Доп. компоненты — ext-links, reglaments, sedo, client-data, theme (заглушки/реализация)
10. ✅ Главный файл — `app.js` и точка входа `entry.js`
11. ✅ Подключение в HTML — после `script.js` подключается `<script type="module" src="./js/entry.js">`

## 🔄 Текущее состояние

- **script.js** по-прежнему основной исполняемый файл (инициализация БД, UI, логика).
- **entry.js** загружается после script.js и вызывает `setDependencies(window)`, передавая в новые модули глобальные функции и объекты. Повторная инициализация БД из модулей не выполняется.
- Модули готовы к постепенной миграции: в script.js можно по частям заменять код на импорты из `js/*`.

## 📝 Как подключено в HTML

```html
<script src="./script.js"></script>
<script type="module" src="./js/entry.js"></script>
```

Порядок: сначала выполняется script.js (все глобальные функции в window), затем модуль entry.js связывает их с компонентами через `setDependencies`.

## 🧪 Тестирование

1. Открывать приложение через локальный сервер (ES-модули не работают при открытии file://):
   - `npx serve .` или `python -m http.server` в корне проекта.
2. Открыть в браузере указанный адрес (например http://localhost:3000).
3. В консоли должно появиться: `[entry.js] Модули связаны с глобальными зависимостями.`
4. Проверить работу: вкладки, закладки, алгоритмы, уведомления, экспорт — без изменений в поведении.

## 📝 Использование модулей в коде (при дальнейшей миграции)

### Использование модулей в коде

```javascript
// Импорт констант
import { DB_NAME, DB_VERSION } from './js/constants.js';

// Импорт функций БД
import { getAllFromIndexedDB, saveToIndexedDB } from './js/db/indexeddb.js';

// Импорт сервисов
import { NotificationService } from './js/services/notification.js';

// Использование
NotificationService.add('Сообщение', 'success');
```

## 🎯 Преимущества новой структуры

1. **Модульность** - код разделен на логические модули
2. **Читаемость** - легче найти нужную функциональность
3. **Поддерживаемость** - проще вносить изменения
4. **Тестируемость** - модули можно тестировать отдельно
5. **Переиспользование** - модули можно использовать в других проектах
6. **Масштабируемость** - легко добавлять новые модули

## ⚠️ Важно

- Все модули используют ES6 модули (`import`/`export`)
- Для работы требуется сервер или локальный сервер разработки
- Браузер должен поддерживать ES6 модули (все современные браузеры поддерживают)

## 📚 Документация модулей

### Константы (`constants.js`)
Содержит все константы приложения: названия БД, ключи localStorage, веса полей для поиска и т.д.

### Конфигурация (`config.js`)
Содержит конфигурационные объекты: настройки вкладок, модальных окон, UI классы.

### База данных (`db/`)
- `indexeddb.js` - основные операции с IndexedDB
- `favorites.js` - работа с избранным
- `stores.js` - конфигурация хранилищ

### Сервисы (`services/`)
- `notification.js` - система уведомлений
- `export.js` - экспорт в PDF

### Компоненты (`components/`)
- `modals.js` — работа с модальными окнами
- `tabs.js` — управление вкладками
- `algorithms.js` — работа с алгоритмами
- `main-algorithm.js` — главный алгоритм
- `bookmarks.js` — работа с закладками
- `ext-links.js` — внешние ресурсы (пока делегирует в window)
- `reglaments.js` — регламенты (пока делегирует в window)
- `sedo.js` — типы СЭДО (пока делегирует в window)
- `client-data.js` — клиентские данные (getClientData, saveClientData, clearClientData)
- `theme.js` — переключение темы (setTheme)

### Утилиты (`utils/`)
- `helpers.js` - вспомогательные функции
- `html.js` - утилиты для работы с HTML
