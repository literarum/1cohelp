# Детальный план миграции из мегафайла

## 📊 Текущий прогресс

### ✅ Завершено

#### 1. CSS миграция (100%)
- ✅ `css/styles.css` - основные стили (1,695 строк)
- ✅ `css/inline-styles.css` - встроенные стили (98 строк)
- ✅ `index.html` уменьшен с 4,415 до 2,247 строк (-49%)

#### 2. HTML шаблоны (100%)
- ✅ 9 HTML шаблонов извлечено в `templates/`
- ✅ `js/ui/template-loader.js` создан

#### 3. JavaScript модули UI (частично)
- ✅ `js/ui/modals-manager.js` (~200 строк) - управление модальными окнами
- ✅ `js/ui/view-manager.js` (~400 строк) - управление видами отображения

### 📈 Статистика
- **До миграции:**
  - `index.html`: 4,415 строк
  - `script.js`: 12,416 строк
- **После миграции:**
  - `index.html`: 2,247 строк (-49%)
  - `script.js`: 11,975 строк (-3.5%)
- **Удалено дублирующего кода:** ~441 строка
- **Создано новых модулей:** ~600 строк

---

## 🎯 План дальнейшей миграции

### Этап 1: Функции рендеринга и отображения (Приоритет: Высокий)

#### 1.1. Рендеринг алгоритмов ✅ ЗАВЕРШЕНО
**Файлы:** 
- `js/components/algorithms.js` - расширен с `renderAllAlgorithms()` и `renderAlgorithmCards()`
- `js/components/main-algorithm.js` - содержит `renderMainAlgorithm()`
- `js/components/algorithms-renderer.js` - новый модуль с `showAlgorithmDetail()`
**Функции мигрированы:**
- ✅ `renderAllAlgorithms()` - в `js/components/algorithms.js`
- ✅ `renderAlgorithmCards()` - в `js/components/algorithms.js`
- ✅ `renderMainAlgorithm()` - в `js/components/main-algorithm.js`
- ✅ `showAlgorithmDetail()` - в `js/components/algorithms-renderer.js`
**Оценка:** ~545 строк
**Статус:** ✅ Завершено
**Результат:** Удалено ~295 строк из `script.js`, создан новый модуль `algorithms-renderer.js`

#### 1.2. Рендеринг закладок
**Файл:** `js/components/bookmarks-renderer.js`
**Функции для миграции:**
- `createBookmarkElement()` (~200 строк)
- `renderBookmarks()` (если есть)
- `addBookmarkToDOM()` (~35 строк)
- `updateBookmarkInDOM()` (~30 строк)
- `removeBookmarkFromDOM()` (~100 строк)
**Оценка:** ~365 строк
**Статус:** ⏳ Ожидает

#### 1.3. Рендеринг регламентов
**Файл:** `js/components/reglaments-renderer.js`
**Функции для миграции:**
- `renderReglamentCategories()` (~100 строк)
- `createCategoryElement()` (~90 строк)
- `getReglamentsByCategory()` (~100 строк)
- `renderReglaments()` (если есть)
**Оценка:** ~290 строк
**Статус:** ⏳ Ожидает

#### 1.4. Рендеринг внешних ссылок
**Файл:** `js/components/ext-links-renderer.js`
**Функции для миграции:**
- `createExtLinkElement()` (~80 строк)
- `renderLinks()` (~100 строк)
- `filterLinks()` (~20 строк)
**Оценка:** ~200 строк
**Статус:** ⏳ Ожидает

---

### Этап 2: Функции инициализации (Приоритет: Высокий)

#### 2.1. Инициализация UI
**Файл:** `js/ui/init.js`
**Функции для миграции:**
- `initUI()` (~3 строки, но вызывает много других)
- `initViewToggles()` - уже мигрировано ✅
- `initStepInteractions()` (~35 строк)
- `initCollapseAllButtons()` (~30 строк)
- `initStepSorting()` (~5 строк)
- `initHotkeysModal()` (~150 строк)
**Оценка:** ~220 строк
**Статус:** ⏳ Ожидает

#### 2.2. Инициализация систем
**Файл:** `js/ui/systems-init.js`
**Функции для миграции:**
- `initBookmarkSystem()` (~430 строк)
- `initCibLinkSystem()` (~5 строк)
- `initCibLinkModal()` (~8 строк)
- `initReglamentsSystem()` (~10 строк)
- `initUICustomization()` (~360 строк)
- `initClearDataFunctionality()` (~200 строк)
**Оценка:** ~1,013 строк
**Статус:** ⏳ Ожидает

---

### Этап 3: Функции работы с данными (Приоритет: Средний)

#### 3.1. Работа с алгоритмами
**Файл:** `js/components/algorithms-operations.js`
**Функции для миграции:**
- `editAlgorithm()` (~200 строк)
- `showAddModal()` (~65 строк)
- `addNewStep()` (~5 строк)
- `addEditStep()` (~5 строк)
- `extractStepsDataFromEditForm()` (~5 строк)
**Оценка:** ~280 строк
**Статус:** ⏳ Ожидает

#### 3.2. Работа с закладками
**Файл:** `js/components/bookmarks-operations.js`
**Функции для миграции:**
- `showAddBookmarkModal()` (если есть)
- `showBookmarkDetail()` (если есть)
- `showOrganizeFoldersModal()` (~20 строк)
- `filterBookmarks()` (~60 строк)
- `populateBookmarkFolders()` (~20 строк)
**Оценка:** ~100 строк
**Статус:** ⏳ Ожидает

#### 3.3. Работа с регламентами
**Файл:** `js/components/reglaments-operations.js`
**Функции для миграции:**
- `handleReglamentAction()` (~150 строк)
- `populateReglamentCategoryDropdowns()` (~8 строк)
**Оценка:** ~158 строк
**Статус:** ⏳ Ожидает

#### 3.4. Работа с внешними ссылками
**Файл:** `js/components/ext-links-operations.js`
**Функции для миграции:**
- `showAddExtLinkModal()` (~100 строк)
- `showOrganizeExtLinkCategoriesModal()` (~50 строк)
- `filterExtLinks()` (~30 строк)
- `handleLinkAction()` (~30 строк)
- `handleLinkActionClick()` (~40 строк)
**Оценка:** ~250 строк
**Статус:** ⏳ Ожидает

---

### Этап 4: Утилиты и вспомогательные функции (Приоритет: Низкий)

#### 4.1. Работа с шагами алгоритмов
**Файл:** `js/utils/steps-utils.js`
**Функции для миграции:**
- `toggleStepCollapse()` - уже в модуле ✅
- `attachScreenshotHandlers()` (~5 строк)
- `renderTemporaryThumbnail()` (~10 строк)
- `renderScreenshotIcon()` (~35 строк)
**Оценка:** ~50 строк
**Статус:** ⏳ Ожидает

#### 4.2. Работа со скриншотами
**Файл:** `js/utils/screenshots-utils.js`
**Функции для миграции:**
- `renderScreenshotThumbnails()` (~5 строк)
- `renderScreenshotList()` (~300 строк)
- `attachBookmarkScreenshotHandlers()` (~700 строк)
**Оценка:** ~1,005 строк
**Статус:** ⏳ Ожидает

#### 4.3. Работа с модальными окнами (дополнительно)
**Файл:** `js/ui/modals-utils.js`
**Функции для миграции:**
- `showNoInnModal()` (~60 строк)
- `requestCloseModal()` (~100 строк)
- `closeModalOnEscapeWithPropagationStop()` (~60 строк)
**Оценка:** ~220 строк
**Статус:** ⏳ Ожидает

#### 4.4. Работа с темами
**Файл:** `js/ui/theme-utils.js`
**Функции для миграции:**
- `setTheme()` (~4 строки)
- `applyThemeOverrides()` (~10 строк)
- `migrateLegacyThemeVars()` (~4 строки)
**Оценка:** ~18 строк
**Статус:** ⏳ Ожидает

#### 4.5. Работа с клиентскими данными
**Файл:** `js/utils/client-data-utils.js`
**Функции для миграции:**
- `getClientData()` - уже в модуле ✅
- `loadClientData()` - уже в модуле ✅
- `clearClientData()` - уже в модуле ✅
- `applyClientNotesFontSize()` (~10 строк)
- `createClientNotesInnPreview()` (~250 строк)
**Оценка:** ~260 строк
**Статус:** ⏳ Ожидает

---

### Этап 5: Обработчики событий (Приоритет: Средний)

#### 5.1. Горячие клавиши
**Файл:** `js/ui/hotkeys-handler.js`
**Функции для миграции:**
- `setupHotkeys()` (~60 строк)
- `handleGlobalHotkey()` (~1,030 строк)
- `navigateBackWithinApp()` (~25 строк)
- `handleNoInnLinkEvent()` (~12 строк)
**Оценка:** ~1,127 строк
**Статус:** ⏳ Ожидает

#### 5.2. Обработчики действий
**Файл:** `js/ui/actions-handler.js`
**Функции для миграции:**
- `handleNoInnLinkClick()` (~100 строк)
- `handleReglamentAction()` - уже упомянуто выше
- `handleLinkAction()` - уже упомянуто выше
**Оценка:** ~100 строк
**Статус:** ⏳ Ожидает

---

## 📊 Итоговая оценка

### Общий объем кода для миграции:
- **Этап 1 (Рендеринг):** ~1,485 строк
- **Этап 2 (Инициализация):** ~1,233 строки
- **Этап 3 (Операции):** ~788 строк
- **Этап 4 (Утилиты):** ~1,553 строки
- **Этап 5 (Обработчики):** ~1,227 строк

**Всего:** ~6,286 строк кода можно мигрировать

### Ожидаемый результат:
- `script.js`: уменьшится с 11,975 до ~5,700 строк (-52%)
- Будет создано ~15-20 новых модулей
- Код станет более модульным и поддерживаемым

---

## 🎯 Приоритеты выполнения

1. **Высокий приоритет:**
   - Этап 1: Рендеринг (критично для UI)
   - Этап 2: Инициализация (критично для работы приложения)

2. **Средний приоритет:**
   - Этап 3: Операции с данными
   - Этап 5: Обработчики событий

3. **Низкий приоритет:**
   - Этап 4: Утилиты (можно делать постепенно)

---

## 📝 Следующие шаги

1. ✅ Составлен детальный план миграции
2. ⏳ Начать с Этапа 1.1: Рендеринг алгоритмов
3. ⏳ Продолжить с Этапом 1.2: Рендеринг закладок
4. ⏳ И так далее по плану...

---

**Дата создания плана:** 6 февраля 2026  
**Текущий прогресс:** ~10% от общего объема миграции  
**Ожидаемое время завершения:** Постепенно, по мере выполнения этапов
